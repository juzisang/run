class ModernBase {

  /**
   * 加载需要的依赖
   */
  onBind (config) {
  }

  /**
   * 开始运行
   */
  onStart (config) {
  }

  /**
   * 运行成功
   */
  onRun (config) {
  }

  /**
   * 运行失败
   */
  onError (error) {
  }
}

module.exports = ModernBase
