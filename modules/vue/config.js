const path = require('path')

module.exports = {
  // paths
  baseUrl: '/',
  outputDir: 'dist',
  // devtool
  devtool: '#source-map',
  // open Browser
  autoOpenBrowser: false,
  // Source Maps
  sourceMap: true,
  configureWebpack: {},
  devServer: {}
}